# Recheck of earlier fixes, 24 September 2026

Each line of the recheck list in `RECHECK_AND_GOAL.md`, checked by its test,
and by a render where it's visual. Headless Linux build, Godot 4.7.2.

## Passed by test (all exit 0)

- **The opening and intake:** opening, intake_gui, intake_body,
  intake_direction, face_model, vat_containment, lower_works_route,
  clothing_shell, clothing, opening_handoff, opening_stage_wiring,
  breakable_door.
- **Hollis and death:** opening_death, vat_rebirth,
  rebirth_every_killer, drain_stalker, support_unit, derby_loss_capture.
- **Ways out:** first_thirty_route, old_drains_route, doctor_route,
  doctor_vehicle_bay, derby_exit, blood_waterfall_exit.
- **The Hunt:** arrival_ease, perspective_unlock, nerve_rig, skin_case,
  case_menu, outfit, body_forms, anatomy_presentation, brain_hub,
  hud_settings.
- **Combat, new:** blood_moves, combat_tiers_hunt, sparring,
  blood_ledger_hunt.

## Seen in renders

- Intake: the receipt tabs print in, the stats are gauges, and the answer
  box has blink icons and a transcript.
- END ALL SUFFERING and GET REVENGE both show as the animated title cards.
- The escape objective reads GET REVENGE // HE LEFT THROUGH THAT DOOR.
- The capture itself left the END ALL SUFFERING card and title on screen,
  because it skips the wires. Fixed in the capture, not the game: in play
  both clear on their own.

## Fixed

- **Ways out: all four now reach the Hunt at their own place.**
  `first_thirty_route_test` covered two routes; it now covers four.
- **Found while extending it:** the derby's secret route came out at the
  dry falls, then spawned you in the Hunt at the default point, because
  the falls filed no handoff. The falls now file one at their surface point
  (`FacilityRoutes.hand_off_at`). A route's own handoff, such as the old
  drains, still wins.

## Still to check by playing, on Greg's machine

- Whether the examiner's face reads as ordinary, and whether blood shows on
  his coat at full size.
- The breakout puddle and the Lower Works lighting.
- How long the X-ray loading screen stays up after you arrive.
- Whether Hollis's warning, wound and kill sequence reads in play.
